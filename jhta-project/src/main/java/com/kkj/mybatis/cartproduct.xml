<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kkj.mybatis">
<select id="selectListCart" resultType="CartProductDto">
	SELECT cp.cart_item_id AS cartitemid, p.PDTTHUM, p.PDTNAME , p.PDTPRICE, cp.quantity  FROM cart_pdt cp 
		Left OUTER JOIN product P
			ON cp.pdtid = p.pdtid
				order by cp.cart_item_id desc
</select>
	<insert id="insertCart" parameterType="CartProductDto">
		insert into cart_pdt values (
				seq_cart_pdt.nextval,
				#{pdtId},
				#{quantity},
				1
		)
	</insert>
</mapper>