<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kkj.mybatis">

	<select id="listProduct" parameterType="HashMap"
		resultType="ProductDto">
		SELECT a.* FROM
		(SELECT p.*, rownum AS rn FROM product p
		ORDER BY pdtid DESC) a
		WHERE rn BETWEEN #{start} AND #{end}
	</select>

	<select id="detailProduct" parameterType="int"
		resultType="ProductDto">
		SELECT * FROM product where pdtId = #{pdtId}
	</select>

	<select id="selectOneProduct" parameterType="int"
		resultType="ProductDto">
		select * from product where pdtid = #{pdtId}
	</select>

	<insert id="insertProduct" parameterType="ProductDto">
		INSERT INTO product
		values(
		seq_product.nextval,
		#{pdtName},
		#{pdtCty},
		#{pdtContent},
		#{pdtPrice},
		default,
		default,
		null,
		#{pdtThum},
		#{pdtColor}
		)
	</insert>

	<update id="updateProduct" parameterType="ProductDto">
		UPDATE PRODUCT SET
		pdtname = #{pdtName},
		pdtcty = #{pdtCty},
		pdtcontent = #{pdtContent},
		pdtprice = #{pdtPrice},
		pdtstate = #{pdtState},
		pdtmoddate = sysdate,
		pdtthum = #{pdtThum},
		pdtColor = #{pdtColor}
		WHERE pdtId = #{pdtId}
	</update>

	<delete id="deleteProduct" parameterType="int">
		delete from product
		where pdtid=#{pdtId}
	</delete>

	<select id="countProduct" resultType="int">
		select count(*) from
		product
	</select>

	<select id="searchProduct" parameterType="HashMap"
		resultType="ProductDto">
		SELECT a.* from
		(SELECT p.*, rownum rn FROM product p WHERE
		<choose>
			<when test="category=='pdtName'">
				pdtName LIKE '%'||#{keyword}||'%'
			</when>
			<when test="category=='pdtColor'">
				pdtColor LIKE '%'||#{keyword}||'%'
			</when>
			<when test="category=='pdtCty'">
				pdtCty LIKE '%'||#{keyword}||'%'
			</when>
			<when test="category=='pdtPrice'">
				pdtPrice = #{keyword}
			</when>
			<when test="category=='all'">
				(pdtName LIKE '%'||#{keyword}||'%') or
				(pdtColor LIKE
				'%'||#{keyword}||'%') or
				(pdtCty LIKE '%'||#{keyword}||'%') or
				(pdtPrice = #{keyword})
			</when>
		</choose>
		ORDER BY p.pdtid DESC) a
		WHERE rn BETWEEN #{start} AND #{end}
	</select>

	<select id="searchProductCount" parameterType="HashMap"
		resultType="int">
		SELECT count(*) as count from
		(SELECT p.*, rownum rn FROM product p
		WHERE
		<choose>
			<when test="category=='pdtName'">
				pdtName LIKE '%'||#{keyword}||'%'
			</when>
			<when test="category=='pdtColor'">
				pdtColor LIKE '%'||#{keyword}||'%'
			</when>
			<when test="category=='pdtCty'">
				pdtCty LIKE '%'||#{keyword}||'%'
			</when>
			<when test="category=='pdtPrice'">
				pdtPrice = #{keyword}
			</when>
			<when test="category=='all'">
				(pdtName LIKE '%'||#{keyword}||'%') or
				(pdtColor LIKE
				'%'||#{keyword}||'%') or
				(pdtCty LIKE '%'||#{keyword}||'%') or
				(pdtPrice = #{keyword})
			</when>
		</choose>
		)
	</select>
</mapper>