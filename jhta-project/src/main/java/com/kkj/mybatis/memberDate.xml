<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.khk11.mybatis">
	<update id="loginDate" parameterType="String">
  		UPDATE controlDate SET loginDate = SYSDATE, datNo = datNo+1 where userID =#{userID}
  	</update>
  	<insert id="insertDate" parameterType="String">
  		insert into controlDate values(#{userID},null,null,0)
  	</insert>
  	<select id="loginCount" resultType="int">
  		select sum(datNo) as count from controlDate where TO_DATE(loginDate,'YYYY-mm-dd') = TO_DATE(sysdate,'YYYY-mm-dd')
  	</select>
</mapper>